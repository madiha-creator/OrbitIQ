# Integrations (INT-*)

Owned jointly with Backend — see `backend/README.md` for the task list (INT-001–004), since these tools are implemented as part of the backend service. This folder is for integration-specific config, credentials setup docs (no secrets), and any standalone connector code (e.g. Salesforce/Jira SDK wrappers) that the backend imports.
