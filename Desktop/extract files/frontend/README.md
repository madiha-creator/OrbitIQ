# Frontend (FE-*)

Mobile worker app + supervisor dashboard.

## Tasks
- **FE-001** — Build mode-aware app UI: procedure-step view / report-field view
- **FE-002** — Build live transcript + safety-alert banner UI
- **FE-003** — Build text/keyboard fallback input path (accessibility)
- **FE-004** — Build supervisor review UI: approve/edit/reject actions

## Notes
- A Macaly-built UI/design prototype exists for reference (mobile worker app + supervisor dashboard) — ask the project owner for the link/export if you don't have it.
- The safety-alert banner (FE-002) needs to interrupt/override normal playback state — coordinate with `audio/` and `safety/`.
